CommunityService:
  allOf:
    - $ref: '#/components/schemas/LightCommunityService'
      type: object
      properties:
        close:
          $ref: "#/components/schemas/LightClose"
      required:
        - close
